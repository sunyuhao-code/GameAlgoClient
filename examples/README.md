# GameAlgo 客户端示例

这里放游戏团队可直接运行的接入示例。

计划示例：

- iOS demo
- Android demo
- REST API curl 示例
- REST API 最小后端示例

示例只能使用公开 SDK API 和 `/v1/*` 接口。

当前已有示例：

- `../rest-api/examples/node/basic.ts`
- `web-game-demo/`
- `curl/`
