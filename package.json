{
  "name": "gamealgo-client-sdk",
  "version": "0.0.0",
  "private": true,
  "description": "GameAlgo client SDK development workspace",
  "type": "module",
  "scripts": {
    "check": "node --test rest-api/src/*.test.ts"
  },
  "engines": {
    "node": ">=24"
  }
}
