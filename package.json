{
  "name": "gamealgo-client",
  "version": "0.1.0",
  "private": true,
  "description": "GameAlgo platform SDKs and protocol fixtures",
  "type": "module",
  "scripts": {
    "build:runtime": "cargo build --locked --manifest-path runtime/rust/Cargo.toml",
    "check": "npm run build:runtime && node --test rest-api/src/*.test.ts"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0"
  }
}
